name: Lint & Test
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main


jobs:
  verify:
    uses: ./.github/workflows/reusable_verify.yaml
    with:
      build: true
      lint: true
      test: false
